name: Deploy to GitHub Pages

on: 
  push:
    branches: 
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2
    
    - name: Build
      uses: actions/setup-node@v1
      with:
        node-version: '13.x' 
    - run: |
        npm install
        npm test
        npm run build
        
    - name: Deploy
      uses: peaceiris/actions-gh-pages@v2
      env:
        ACTIONS_DEPLOY_KEY: ${{ secrets.GITHUB_TOKEN }}
        EXTERNAL_REPOSITORY: doblado/dobladov.github.io
        PUBLISH_BRANCH: master
        PUBLISH_DIR: ./public
